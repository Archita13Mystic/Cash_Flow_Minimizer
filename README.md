# Cash_Flow_Minimizer
Cash Flow Minimizer built in C++. Given a set of transactions between people, it computes net balances and finds the optimal repayment plan that settles all debts in the fewest possible transactions. Supports custom names, manual input, and a quick demo mode.
